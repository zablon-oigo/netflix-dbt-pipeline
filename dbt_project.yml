name: 'netflix'
version: '1.0.0'

profile: 'netflix'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:     
  - "target"
  - "dbt_packages"


models:
  netflix:
    +materialized: view 
    staging:
      +materialized: view 
    dim:
      +materialized: table
    fct:
      +materialized: table
    marts:
      +materialized: table

seeds:
  netflix:
    movie_release_dates:
      column_types:
        movie_id: integer
        release_date: date